# 🚀 Labs First Semester on JavaScript (First Semester)

This repository contains basic JavaScript labs covering algorithms, data structures, and functional programming fundamentals.

This is the First Semester repository. I've been making edits to it, so it has two branches:
First: Master (the branch for the entire First Semester's work) and Main (currently the Front Branch).

---

## 📌 Completed works:

### 🔹 [lab-01] Data Types and Memory Management
Exploring the differences between scalar (primitive) and reference data types in JavaScript. Implementing arguments passing by value and by reference. Writing an algorithm for dynamically counting the frequency of different data types in a mixed array. Setting up a basic working environment.
* **📂 Structure:** `lab-01/Exercises/` (solution source code).
* **▶️ Running tests:** `cd lab-01 && node <file_name>.js`

### 🔹 [lab-02] Functions, Loops, and Data Structures
Working with scopes, variables, and constants. Using loops to generate arrays with numeric ranges. Function composition (nested calls). Comparison of data structures: implementing a phone book using an array of objects and a hash table (Map).
* **📂 Structure:** `lab-02/Exercises/` (solution source code).
* **▶️ Running tests:** `cd lab-02 && node <file_name>.js`

### 🔹 [lab-03] Basic syntax and data structures
Practice using loops to generate numerical sequences. Study of passing data between functions. Working with objects (mutation) and implementing search algorithms: linear search through an array of objects and fast search by key in a hash table.
* **📂 Structure:** `lab-03/Exercises/` (source code for solutions).
* **▶️ Running tests:** `cd lab-03 && node <file_name>.js`

### 🔹 [lab-04] Loops and Iteration of Data Structures
Learning various iteration methods in JavaScript. Practicing classic loops (`for`, `while`, `do..while`, `for..of`) and the `reduce` method for data aggregation. Implementing algorithms for traversing multidimensional arrays (finding an extremum) and transforming complex reference objects using `for..in`.
* **📂 Structure:** `lab-04/Exercises/` (source code for solutions).
* **▶️ Running tests:** `cd lab-04 && node <file_name>.js`

### 🔹 [lab-05] Closures & Chaining
Using closures to implement the chaining pattern—building chains of deferred calls for sequential data processing. Creating a functional wrapper over an array that securely encapsulates its state and provides a secure interface.
* **📂 Structure:** `lab-05/Exercises/` (solution source code).
* **▶️ Running tests:** `cd lab-05 && node <file_name>.js`

### 🔹 [lab-06] Functional Programming and Composition
Advanced work with higher-order functions. Implementation of the `pipe` (left-to-right composition) and `compose` (right-to-left composition) architectural patterns. Implementation of strict argument type checking and safe exception handling.
* **📂 Structure:** `lab-06/` (logic modules).
* **▶️ Running the script:** `cd lab-06 && node <file_name>.js` 

### 🔹 [lab-07] Advanced Array Methods
Creating custom utilities for array manipulation. Implementing functions for selective and multiple element removal (using the rest operator). Writing basic filtering algorithms: removing duplicates from collections and calculating the difference between two arrays.
* **📂 Structure:** `lab-07/` (logic modules).
* **▶️ Running the script:** `cd lab-07 && node <file_name>.js` 

### 🔹 [lab-08] Higher-Order Functions
Developing custom object iterators via callbacks and using closures to safely store state. Creating a wrapper function (`contract`) for strict runtime type checking of received arguments and return values ​​with exception throwing.
* **📂 Structure:** `lab-08/` (logic modules).
* **▶️ Running the script:** `cd lab-08 && node <file_name>.js` 